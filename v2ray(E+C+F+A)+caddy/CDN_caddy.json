//使用多个域名实现CDN流量中转与正常应用共存配置
//一、多个域名申请普通TLS证书配置
//原配置：
//.........<省略>........
            "match": [{
              "host": ["xx.yy"] //限定域名访问（禁止以IP方式访问网站），修改为自己的域名。
            }],
//.........<省略>........
      "certificates": {
        "automate": ["xx.yy"] //自动化管理TLS证书（包括获取、更新及加载证书）。修改为自己的域名。
      },
//.........<省略>........

//以上配置修改为如下：
//.........<省略>........
            "match": [{
              "host": ["z1.xx.yy","z2.xx.yy"] //限定域名访问（禁止以IP方式访问网站），修改为自己的域名。
            }],
//.........<省略>........
      "certificates": {
        "automate": ["z1.xx.yy","z2.xx.yy"] //自动化管理TLS证书（包括获取、更新及加载证书）。修改为自己的域名。
      },
//.........<省略>........
//备注：
//1、申请免费TLS证书的域名不要超过五个，否则影响TLS证书的更新。
//2、从Let's Encrypt申请的普通TLS证书在‘/home/tls/certificates/acme-v02.api.letsencrypt.org-directory/z1.xx.yy’目录中；z1.xx.yy为域名，根据域名变化。
//3、从ZeroSSL申请的普通TLS证书在‘/home/tls/certificates/acme.zerossl.com-v2-dv90/z1.xx.yy’目录中；z1.xx.yy为域名，根据域名变化。


//二、申请通配符TLS证书配置
//从Let's Encrypt或ZeroSSL申请通配符TLS证书配置，详见‘caddy(other configuration) （Caddy的特殊应用配置方法。）’中对应介绍及对应caddy-dns插件配置示例。
