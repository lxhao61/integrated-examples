{
	order forward_proxy before route
	order reverse_proxy before forward_proxy
	admin off
	log {
		output file /var/log/caddy/error.log
		level ERROR
	} #版本不小于v2.4.0才支持日志全局配置，否则各自配置。
	storage file_system {
		root /home/tls #存放TLS证书的基本路径
	}

	cert_issuer acme #acme表示从Let's Encrypt申请TLS证书，zerossl表示从ZeroSSL申请TLS证书。必须acme与zerossl二选一（固定TLS证书的目录便于引用）。注意：版本不小于v2.4.1才支持。
	email your@email.com #电子邮件地址。选配，推荐。

	servers unix//dev/shm/h1h2c.sock { #与下边监听进程对应
		listener_wrappers {
			proxy_protocol #开启PROXY protocol接收
		}
		protocols h1 h2c #开启HTTP/1.1 server与H2C server支持
	}
	servers unix//dev/shm/https.sock { #与下边监听进程对应
		listener_wrappers {
			proxy_protocol #开启PROXY protocol接收
			tls #HTTPS server开启PROXY protocol接收必须配置
		}
		protocols h1 h2 #关闭HTTP/3 server支持（HTTP/3 server不支持UDS监听）
	}
}

:80 { #HTTP默认监听端口
	redir https://{host}{uri} permanent #HTTP自动跳转HTTPS，让网站看起来更真实。
}

:88 { #绑定UDS后此端口失效，仅做标记作用。
	bind unix//dev/shm/h1h2c.sock #绑定UDS（HTTP/1.1 server及H2C server监听进程）

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" #启用HSTS
	}
	file_server {
		root /var/www/html #修改为自己存放的WEB文件路径
	}
}

:7443, zv.xx.yy:7443, zt.xx.yy:7443, zh.xx.yy:7443 { #绑定UDS后此端口失效，仅做标记作用。zv.xx.yy、zt.xx.yy、zh.xx.yy修改为自己的域名。注意：逗号与域名（或含端口）之间有一个空格。
	bind unix//dev/shm/https.sock #绑定UDS（HTTPS server监听进程）

	tls {
		ciphers TLS_AES_256_GCM_SHA384 TLS_AES_128_GCM_SHA256 TLS_CHACHA20_POLY1305_SHA256 TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384 TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256 TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256
		curves x25519 secp521r1 secp384r1 secp256r1
	}

	reverse_proxy /SeuW56Es unix//dev/shm/vh2c.sock { #与vless+h2c应用中path对应，转发给本机vless+h2c监听进程。
		transport http {
			versions h2c 2
		}
	}

	@sgrpc {
		protocol grpc
		path /SALdGZ9k/* #与shadowsocks+grpc应用中serviceName对应
	}
	reverse_proxy @sgrpc 127.0.0.1:2011 { #转发给本机shadowsocks+grpc监听端口
		transport http {
			versions h2c 2
		}
		flush_interval -1
		header_up X-Real-IP {remote_host}
	}

	forward_proxy {
		basic_auth user pass #NaïveProxy用户与密码，修改为自己的。
		hide_ip
		hide_via
		probe_resistance
	}

	route {
		header {
			Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" #启用HSTS
		}
		file_server {
			root /var/www/html #修改为自己存放的WEB文件路径
		}
	}
}
#备注：
#1、申请免费TLS证书的域名不要超过五个，否则影响TLS证书的更新。
#2、从Let's Encrypt申请的普通TLS证书在‘/home/tls/certificates/acme-v02.api.letsencrypt.org-directory/zv.xx.yy’目录中；zv.xx.yy为域名，根据域名变化。
#3、从ZeroSSL申请的普通TLS证书在‘/home/tls/certificates/acme.zerossl.com-v2-dv90/zv.xx.yy’目录中；zv.xx.yy为域名，根据域名变化。
#4、从Let's Encrypt或ZeroSSL申请通配符TLS证书配置，详见‘caddy(other configuration) （Caddy的特殊应用配置方法。）’中对应介绍及对应caddy-dns插件配置示例。
#5、因V2Ray不支持‘证书热更新’功能，即V2Ray不会自动识别TLS证书更新并重载TLS证书，当使用Caddy（内置ACME客户端）申请的TLS证书还需进行如下配置：
#1）、使用Unix和类Unix操作系统的Crontab指令定时重启V2Ray重载更新后的TLS证书。（通用解决办法）
#2）、使用Caddy的events应用配置实现专业ACME客户端acme.sh的reloadcmd参数应用，即TLS证书自动更新后就执行重启V2Ray重载更新后的TLS证书。详见‘caddy(other configuration) （Caddy的特殊应用配置方法。）’中对应介绍及对应配置示例。
