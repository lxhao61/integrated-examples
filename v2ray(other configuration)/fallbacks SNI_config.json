//一、与caddy2对应的Xray回落配置：
//1、Xray常规配置：
.........<省略>........
        "fallbacks": [
          {
            "dest": 88, //dest h2与http/1.1回落共用此端口，回落到web服务器。需web服务器同一端口，同时支持h2c server与http/1.1 server应用。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
.........<省略>........
        ]
//2、修改Xray常规配置如下：（示例仅配置了两个域名的分流；如需要多个域名，增加对应参数即可。）
.........<省略>........
        "fallbacks": [
          {
            "name": "zv.xx.yy",//修改成自己分流的域名
            "dest": 818, //h2与http/1.1回落共用此端口，回落到web服务器。需web服务器同一端口，同时支持h2c server与http/1.1 server应用，端口不能相同。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
          {
            "name": "zt.xx.yy",//修改成自己分流的域名
            "dest": 828, //h2与http/1.1回落共用此端口，回落到web服务器。需web服务器同一端口，同时支持h2c server与http/1.1 server应用，端口不能相同。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
.........<省略>........
        ]
注意："dest"为进程，配置类似。

//二、与nginx对应的Xray回落配置：
//1、Xray常规配置：
.........<省略>........
        "fallbacks": [
          {
            "alpn": "h2", //h2回落匹配
            "dest": 82, //h2回落端口，端口不能相同。
            "xver": 0 //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
          {
            "dest": 81, //http/1.1回落端口，端口不能相同。
            "xver": 0 //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
.........<省略>........
        ]
//2、修改Xray常规配置如下：（示例仅配置了两个域名的分流；如需要多个域名，增加对应参数即可。）
.........<省略>........
        "fallbacks": [
          {
            "name": "zv.xx.yy",//修改成自己分流的域名
            "alpn": "h2", //h2回落匹配
            "dest": 812, //h2回落端口，端口不能相同。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
          {
            "name": "zv.xx.yy",//修改成自己分流的域名
            "dest": 811, //http/1.1回落端口，端口不能相同。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
          {
            "name": "zt.xx.yy",//修改成自己分流的域名
            "alpn": "h2", //h2回落匹配
            "dest": 822, //h2回落端口，端口不能相同。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
          {
            "name": "zt.xx.yy",//修改成自己分流的域名
            "dest": 821, //http/1.1回落端口，端口不能相同。
            "xver": 0  //专用于发送真实来源IP和端口。1或2表示PROXY protocol插件版本；默认为0，即不发送。
          },
.........<省略>........
        ]
注意："dest"为进程，配置类似。
